package dataflow

import scala.util.continuations._

package object io {
  
//	implicit def ByteBufferToByteArray(buf: java.nio.ByteBuffer): Array[Byte] = {
//		val bytes = new Array[Byte](buf.remaining) 
//		buf.get(bytes)
//		bytes
//	}
}